# gps-nav-sim
Simulates satellites in orbit providing GPS data to a vehicle (on Earth) to help it navigate to a goal location

# running
- To run the monolthic version simply do: cargo run --bin monolithic
- To run the multi application version do: docker compose up -d
